export enum JobQueue {
  SIMULATION = "Simulation",
  AUDIT_LOG = "AuditLog",
}

export enum DroneState {
  IDLE = "IDLE",
  LOADING = "LOADING",
  LOADED = "LOADED",
  DELIVERING = "DELIVERING",
  DELIVERED = "DELIVERED",
  RETURNING = "RETURNING",
}
